language: ruby
rvm:
  - "2.2.1"
bundler_args: --without development
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq libsndfile1-dev imagemagick libsox-fmt-all lame mp3val sox madplay twolame flac ffmpeg
services: mysql
before_script:
  - mysqladmin create fixer_prx_org_test -h 127.0.0.1
